{% extends "app.twig" %}

{% block headAssets %}<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>{% endblock %}
{% block title %}Platforms Index{% endblock %}

{% block content %}
    
    {% if hasMessages() %}
        {% if successMessage() %}
            <script>swal("Success!", "{{successMessage()}}", "success");</script>
        {% elseif errorMessage() %}
            <script>swal("Error!", "{{errorMessage()}}", "error");</script>
        {% endif %}
    {% endif %}

    <a href="platforms/create" class="btn btn-success mb-5 float-right"><i class="bi bi-plus"></i> New</a>

    {% if platforms %}
        <table class="table table-striped table-dark table-bordered table-responsive" data-toggle="table" data-pagination="true" data-search="true">
            <thead>
                <tr>
                    <th class="mobile-hidden" data-sortable="true">ID</th>
                    <th data-sortable="true">Name</th>
                    <th class="mobile-hidden text-center" data-sortable="true">Created</th>
                    <th class="actions"></th>
                </tr>
            </thead>
            <tbody>
            {% for platform in platforms %}

                <tr>
                    <td class="mobile-hidden">
                        {{ platform.id }}
                    </td>
                    <td class="primary-mobile-td">
                        {{ platform.name }}
                    </td>
                    <td class="mobile-hidden text-center">
                        {{ platform.created_at|date("m/d/Y") }}
                    </td>
                    <td class="mobile-td text-center">
                        <a href="platforms/{{ platform.id }}/edit" class="btn btn-primary"><i class="bi bi-pencil"></i><span class="mobile-hidden"> Edit</span></a>
                        <a href="platforms/{{ platform.id }}" class="btn btn-secondary"><i class="bi bi-eye"></i><span class="mobile-hidden"> View</span></a>
                        <a href="javascript:FunApp.deleteResource({{ platform.id }}, 'platform', 'delete-form-{{ platform.id }}')" class="btn btn-danger"><i class="bi bi-trash"></i><span class="mobile-hidden"> Delete</span></a>
                        <form method="post" action="/platforms/{{ platform.id }}/delete" id="delete-form-{{ platform.id }}"></form>
                    </td>
                </tr>

            {% endfor %}
            </tbody>
        </table>
    {% else %}
        No platforms found.
    {% endif %}

{% endblock %}